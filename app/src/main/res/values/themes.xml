<resources>
    <style name="Theme.Pokemon" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryContainer">@color/purple_700</item> <!-- Alternative to colorPrimaryVariant -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Accent color -->
        <item name="colorSecondary">@color/teal_200</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/white</item>

        <!-- Text appearance -->
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
